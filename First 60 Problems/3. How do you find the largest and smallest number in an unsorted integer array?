#include <bits/stdc++.h>
using namespace std;
void smallAndLarge(int a[],int n)
{
    sort(a,a+n);
    cout<<a[0]<<" "<<a[n-1];
}
int main() {
	int a[]={12, 11, 40, 12, 5, 6, 5, 12, 11};
	int n=sizeof(a)/sizeof(a[0]);
	smallAndLarge(a,n);
}
